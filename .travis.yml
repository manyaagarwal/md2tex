language: node_js
branches:
  only:
    - master
node_js:
  - "10"
  - "12"
  - "lts/*"
install:
  - yarn install --production=false
  - yarn global add codecov
script:
  - yarn test
  - codecov
  - yarn build
deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  skip_cleanup: true
notifications:
  email: false
cache: yarn
